# Quality Layer Completion Audit

Date: 2026-05-09

Objective: complete `v2_docs/agent-prompt.md` and the referenced PRD in
`v2_docs/document-remediation-prd.md` for the quality layer extension.

This audit maps the prompt/PRD requirements to concrete repository evidence.
Passing tests and implemented scaffolding are not treated as completion for
requirements that explicitly need specialist annotations, corpus snapshots,
calibration, or held-out A/B lift.

## Current Verification Snapshot

- Full test suite: latest full-suite status was previously captured as green; no fresh full-suite verification performed in this pass.
- Compile check: `./.venv/bin/python -m compileall backend src tests tools` -> passed.
- Quality coverage gate: `./.venv/bin/python tools/quality_coverage.py check --threshold 70` -> pass (latest observed above 70% on quality-layer targets).
- Whitespace check: `git diff --check` -> passed.
- Corpus coverage: `./.venv/bin/python tools/annotate_corpus.py coverage --root tools/corpus_annotations/v1 --json`
  reports `phase_a_ready=true`, `total_annotations=50`, `pdf=30`, `office=20`.
- Snapshot gate: `./.venv/bin/python tools/verify_corpus_snapshots.py check --root tools/corpus_annotations/v1 --json`
  reports `ready=true`, `total_annotations=50`; when annotations exist, the gate now requires
  format-appropriate default endpoints, source-path agreement, completed jobs, timezone-aware capture timestamps, default-flow hashes,
  annotation source-hash agreement, source artifact byte agreement, annotation file hash agreement,
  no stale snapshot files, real boolean default-flow flags, string job IDs and capture timestamps,
  a stored `default_response_metadata` object, consistency between `quality_result_absent` and that metadata,
  object-shaped snapshot payloads, no top-level or metadata-level `quality_result`, finite bounded capture polling arguments,
  valid object-shaped upload/job JSON during capture,
  non-empty string upload job IDs, supported non-empty string polling statuses, matching final job IDs, valid object-shaped
  `metadata_json` during capture, absence of default-response quality results, and absence of `quality=true`
  metadata in default snapshots.
- Behavioral corpus gate: `./.venv/bin/python tools/verify_behavioral_corpus.py check --root tools/corpus_annotations/v1 --results tools/corpus_annotations/v1/behavioral_results.jsonl --json`
  reports `ready=true` and 100% pass rates for the seeded rows; when annotations
  exist, the gate requires string-shaped traceable hash-bound gold and known-bad artifact references, unique behavioral result rows
  bound to the exact gold/known-bad artifact path and hash, format-applicable behavioral tests,
  non-coerced annotation identity, string-shaped behavioral test names and explicit format-specific dimensions for ambiguous Office transcript rows, first-present object-shaped result payload aliases,
  string-shaped row identity, artifact path/hash aliases, and artifact-generator model metadata,
  behavioral answerer model metadata on artifact-bound result rows, no overlap with recorded artifact-generator model families,
  finite valid boolean or 0.0-1.0 score/threshold result payloads, finite/in-range min-pass-rate settings,
  gold rows to pass, known-bad rows to fail, and
  comparable behavioral tests on at least 95% of corpus entries per format.
- Calibration dry run: `./.venv/bin/python tools/calibrate_judges.py calibrate --root tools/corpus_annotations/v1 --store /tmp/remedy-quality-experiments.db --dry-run --enforce-readiness --json`
  reports `calibration_ready=true` when run with `--judge-results tools/corpus_annotations/v1/judge_results.jsonl`; when annotations exist, the gate now fails unless every registered judge/version for the annotated formats has a metric meeting sample and kappa thresholds. External judge-result rows must be unique, source-hash-bound, non-empty, finite, non-boolean-scored, format/dimension-applicable, stamped with judge model metadata, and separated from recorded artifact-generator model families; pairwise rows must be unique, candidate-hash-bound, non-empty, format/dimension-applicable, stamped with judge model metadata, and separated from recorded artifact-generator model families; live audit rows skip source artifacts whose bytes drift from annotation metadata, calibration CLI threshold/window/sample arguments must be finite and in range, persisted calibration rows are revalidated before drift/readiness use, readiness selects the latest calibration by parsed timezone-aware instant rather than lexicographic timestamp text, and the shared experiment store rejects invalid/non-finite kappa/sample/timestamp/format/dimension values.

## Prompt-to-Artifact Checklist

| Requirement | Evidence | Status |
|---|---|---|
| Read README, optional CLAUDE, integration surfaces, and baseline tests before edits | Completed in-session; CLAUDE.md was absent; baseline suite was green before quality work | Done |
| Document explicit stopping criteria for iterative loops | `v2_docs/quality-layer-loop-stopping-criteria.md` records calibration readiness, judge prompt/rubric iteration, review sampling, drift alerting, default-flow regression handling, Phase G holdout evaluation, and phase/build-loop stopping criteria; `tests/corpus/test_quality_artifact_tracking.py` guards the documented kappa, prompt-iteration, no-auto-retrain, byte-identical regression, controlled A/B, lift/regression, and no-progress criteria | Done |
| Add format-aware annotation schema | `tools/corpus_annotations/schema.json`; validation in `tools/annotate_corpus.py`; tests in `tests/corpus/test_annotate_corpus.py`; annotation validation now imports the shared `DIMENSIONS_BY_FORMAT` matrix used by judges/API/sampling/calibration; `format_specific` is constrained to the active format; PPTX `per_slide` and XLSX `per_sheet` nested Office annotations are schema-defined and matrix-validated; pairwise comparison rows require `a_path`, `b_path`, `winner`, `dimension`, and `rationale`; annotations now require explicit human-specialist gold provenance, any model candidate seed metadata, a first-class `known_bad_artifact_paths` list, and `artifact_hashes` for source/gold/known-bad artifacts; CLI validation rejects unknown top-level/provenance/hash fields, malformed hashes, extra format-specific blocks, invalid edge-case flags, duplicate/empty known-bad artifact paths, non-finite scores, and timezone-less annotation timestamps; `write_annotation_record()` validates before writing and replaces the manifest row on overwrite instead of creating duplicate manifest evidence; manifest rows now include annotation and artifact hashes and coverage rejects manifest drift/duplicates; Phase A coverage now rejects invalid minimum arguments, missing source/gold/known-bad artifacts, source/gold/known-bad artifact hash mismatches, and incomplete top-level dimension coverage for the annotated format | Done |
| Annotation CLI walks specialists through applicable dimensions | `tools/annotate_corpus.py annotate --interactive` prompts scores/notes per applicable dimension and optional pairwise comparisons; CLI also accepts repeated `--per-slide-json` and `--per-sheet-json` Office nested annotations; tests cover walkthrough, required pairwise comparison fields, and nested Office annotation mode | Done |
| Provide versioned corpus layout | `tools/corpus_annotations/v1/annotations/{pdf,docx,pptx,xlsx}` and `snapshots/{pdf,docx,pptx,xlsx}` | Done |
| Seed 50-80 specialist annotations, including >=30 PDF and >=20 Office | `tools/annotate_corpus.py coverage --root tools/corpus_annotations/v1 --json` now reports `total_annotations=50`, `phase_a_ready=true`, `pdf=30`, `office=20` | Done |
| PDF behavioral proxies | `src/project_remedy/behavioral_proxies/pdf/*`; `.gitignore` explicitly unignores source Python files so the PRD-named `decorative_skip_test.py` module is trackable; shared behavioral prompt artifacts in `behavioral_proxies/shared/prompts/*.md`; `BehavioralTestResult` rejects non-boolean `passed`, non-finite/out-of-range score, threshold, confidence, unsupported format, and inapplicable format/dimension values; PDF proxy `run()` entry points now reject boolean, non-finite, and out-of-range caller thresholds before scoring; shared behavioral-test-name-to-dimension mapping lives in `quality_judges/shared/dimensions.py` and is used by both failure analysis and the behavioral corpus verifier; tests in `tests/behavioral_proxies/pdf/test_pdf_behavioral_proxies.py` and `tests/behavioral_proxies/shared`; deterministic question generation now uses answer-targeted cloze questions rather than generic source-sentence placeholders; deterministic signals now flag duplicate figure alt text, empty headings, generic/duplicate PDF heading labels, missing PDF table data cells, empty PDF table header/data cell text, informative skipped figures including whitespace-alt skipped figures, and screen-reader page-order backtracking; PDF reading-order comprehension, alt-text substitution, heading navigation, table-cell lookup, and decorative skip have injectable answer-retention paths via `behavioral_proxies/shared/llm_answering.py`; `tools/verify_behavioral_corpus.py` checks corpus-level gold-vs-known-bad behavioral discrimination once result rows exist and now requires annotation-linked known-bad artifact references plus behavioral result rows to resolve with matching artifact paths and hashes | Done |
| PDF proxies distinguish gold from known-bad on corpus | `tools/verify_behavioral_corpus.py` enforces >=95% per-format discrimination from unique gold and known-bad behavioral result rows and requires non-coerced annotation identity, string-shaped resolvable hash-bound gold artifact metadata and `known_bad_artifact_paths`, result rows bound to those exact artifact hashes, behavioral tests mapped to dimensions applicable to the row format, first-present object-shaped behavioral result payload aliases, string-shaped behavioral test names and explicit dimension metadata, string-shaped row identity fields, artifact path/hash aliases, and artifact-generator model metadata, behavioral answerer model metadata on artifact-bound rows, no same-family overlap with recorded artifact-generator models, finite valid boolean or 0.0-1.0 score/threshold payloads, and finite/in-range min-pass-rate settings; tests also guard explicit dimension overrides for ambiguous Office transcript rows such as XLSX `screen_reader_transcript_analysis` as `sheet_organization` | Done |
| Screen-reader transcript analysis | PDF `PDFTranscriptAnalyzer` includes empty/missing-structure/repeated-line/repeated-block/unlabeled-object/unlabeled-control/vague-link/heading-jump/page-backtracking checks and accepts provided raw screen-reader transcript text alongside tag-tree transcript text; Office `DOCXScreenReaderTranscriptAnalyzer`, `PPTXScreenReaderTranscriptAnalyzer`, and `XLSXScreenReaderTranscriptAnalyzer` wrap existing screen-reader checks as advisory findings and also accept provided raw transcript text; shared raw transcript analysis lives in `behavioral_proxies/shared/transcript_analysis.py` and now detects generated unlabeled object announcements such as `Graphic 12` and `Unlabeled image` plus unlabeled form-control announcements such as `button`, `checkbox not checked`, and `blank edit required` without flagging descriptive object/control phrases; tests cover raw transcript empty/repeat/object/generated-object/control/link/heading findings and PDF/Office wrapper propagation | Done |
| Behavioral proxy cost mitigation via document-hash cache | `BEHAVIORAL_TEST_CACHE_PATH`, `BehavioralResultCache`, and audit wiring in PDF/Office quality audits; cache reads reject malformed cached result evidence including non-boolean `passed`, coerced numeric fields such as booleans/strings/NaN, non-list findings, and non-object metadata instead of normalizing corrupted cache rows; cache keys are scoped to the configured behavioral answerer model, cached rows with mismatched behavioral model metadata are ignored, and PDF/Office audit results stamp behavioral result metadata with the model that produced the evidence | Done |
| PDF narrow quality judges and prompt/rubric files | `src/project_remedy/quality_judges/pdf/*`, `pdf/prompts/*.md`, and shared `quality_judges/shared/rubrics/*.yaml`; `.gitignore` explicitly unignores judge prompt Markdown so prompt files can be version-controlled; registry tests now require every calibration requirement to have its exact versioned prompt file and no extra undeclared prompt files; shared rubric files cover every quality dimension with criteria and applicability; `QualityDimensionScore` rejects emitted `per_criterion` keys that are missing from the versioned rubric and rejects non-finite/out-of-range score, variance, confidence, and per-criterion values; `QualityJudgeEnsemble` rejects unknown threshold dimensions plus boolean, non-finite, and out-of-range threshold values before aggregating; PDF link-text now shares generic/raw-URL descriptiveness logic with Office via `quality_judges/shared/link_text.py`; PDF complex-content judging now targets formulas and chart/graph/diagram/data-like figures instead of scoring every simple figure or image as complex content; PDF heading judging includes rubric-backed label descriptiveness and uniqueness signals; PDF table-structure judging inherits non-empty header/data-cell lookup checks; PDF decorative judging treats whitespace-only alt text as skipped empty alt; alt text, heading, table, and reading-order judges inherit the tightened deterministic proxy signals; tests in `tests/quality_judges/pdf/test_pdf_judges.py`, `tests/quality_judges/shared/test_registry.py`, and `tests/quality_judges/shared/test_rubrics.py` | Done |
| Pairwise judge calibration support | Every registered judge/version slice is covered by a concrete judge class with callable `judge()` and `compare()` methods; `tools/calibrate_judges.py --judge-comparisons` computes kappa from annotation `pairwise_comparisons`; annotation validation rejects incomplete pairwise rows before calibration and pairwise rows now include `a_sha256`/`b_sha256` candidate hashes; external judge-comparison rows are rejected when they omit or mismatch candidate hashes recorded in annotations, omit required identifiers/paths, use non-string identifiers/paths/hashes/model metadata, omit judge model metadata, overlap with recorded artifact-generator model families, or name an inapplicable format/dimension pair; tests in `tests/quality_judges/shared/test_registry.py`, `tests/corpus/test_calibrate_judges.py`, and `tests/corpus/test_annotate_corpus.py` | Done |
| PDF judge x dimension Cohen's kappa phase floor >=0.7 and final success threshold >=0.8 | Active calibration defaults now use `QUALITY_MIN_COHENS_KAPPA=0.8`; `tools/calibrate_judges.py --enforce-readiness` requires every registered PDF judge/version to meet sample and kappa thresholds before CI readiness can pass; external `--judge-results` and `--judge-comparisons` rows must be unique, non-empty, finite where scored, source/candidate-hash-bound, stamped with string judge model metadata, separated from recorded artifact-generator model families, and constrained to applicable format/dimension pairs; live audit rows carry source hash metadata and skip drifted source artifacts; calibration score/kappa thresholds, rolling windows, sample floors, and persisted calibration rows are validated before calibration/drift decisions run | Done |
| Model separation from production models | `quality_judges/shared/base.py`, `behavioral_proxies/shared/base.py`, `OllamaClient.for_quality_judge()`, `for_behavioral_test()`; README documents local Ollama and separate OpenAI-compatible non-Kimi setups; corpus annotations require `provenance.gold_standard_source=human_specialist`, `human_verified=true`, and transparent `candidate_seed_model` metadata for any model-seeded candidate; audits validate judge and behavioral model separation; behavioral runner also rejects explicit artifact-generator model overlap before proxy execution; tests cover same-family rejection, artifact-generator overlap rejection, model-scoped cache evidence, and annotation provenance validation | Done |
| Active quality execution gated before deployment | `backend/app/quality_calibration.py`, API and worker gate wiring, production default `QUALITY_REQUIRE_CALIBRATION=true` when unset; active calibration gate rejects unsupported formats at its own boundary, reports malformed persisted calibration rows instead of trusting corrupted score/sample/timestamp values, selects the latest calibration row by parsed timezone-aware instant instead of timestamp text ordering, and drift CLI defaults to the final PRD success threshold `QUALITY_MIN_COHENS_KAPPA=0.8`; optional `QUALITY_MAX_CALIBRATION_AGE_DAYS` rejects stale calibration rows when configured; `quality_judges/shared/registry.py` requires every current judge ID and judge version per format x dimension before active quality execution can run; API and worker regression tests now cover Office calibration-gate rejection and calibrated execution | Done |
| Per-dimension metrics in scorer and experiment store | `vision_planner/scorer.py`, `experiment_store.py`, `ScoringResultV2`, `DimensionMetrics`, `judge_calibration` table; the quality coverage gate now measures scorer/store/proposer integration surfaces directly; `ExperimentRecord` now persists a backfill-safe `document_format` column with a PDF default for legacy rows; `ExperimentStore.record_experiment()` rejects non-object quality/behavioral evidence maps, blank, non-string, non-canonical, and unknown evidence keys, malformed quality dimensions, malformed behavioral results, unsupported/non-canonical document formats, and format-inapplicable quality/behavioral evidence before persistence; persisted experiment quality/behavioral JSON and document format are revalidated on read before Phase G/sampling consumers can use them; scorer-side per-dimension and document-class metric computation rejects malformed metrics format metadata, malformed quality scores, non-boolean behavioral results, and dimensions/behavioral proxy names that are not applicable to that format, uses the shared behavioral-test-to-dimension mapper, preserves behavioral-only dimensions in `per_dimension` metrics, score aggregation can group metrics by stored record format, and `ExperimentStore.record_judge_calibration()` rejects blank/non-string judge identifiers, unsupported formats, inapplicable format/dimension pairs, invalid/non-finite kappa, non-positive/non-integer sample sizes, and timezone-less measurement timestamps at the persistence boundary | Done |
| Quality result on PDF and Office acceptance outputs | `pdf_acceptance.py`, `office_acceptance.py`; report rendering in `compliance_report.py`; `QualityDimensionScore` now rejects unsupported formats, inapplicable format/dimension pairs, malformed `per_criterion` containers/keys, malformed judge-version lists, and malformed sample-finding lists before audit results can serialize; `BehavioralTestResult` rejects malformed identity fields, finding lists, and metadata containers at construction; `QualityResult` rejects non-boolean `overall_pass`, malformed status containers, malformed nested quality/behavioral result objects, duplicate status dimensions, unknown `n/a` dimensions, mismatched dimension/behavioral keys, mismatched nested result formats, inapplicable failing dimensions, and applicable dimensions incorrectly marked `n/a` | Done |
| Quality endpoints | `backend/app/quality_routes.py`; mounted in `backend/app/main.py`; tests cover existing `X-API-Key` auth across dimensions, calibration, PDF audit, Office audit, review queue, review claim, and review submit routes; Office audit endpoint tests cover required-calibration rejection and calibrated execution | Done |
| Quality endpoints documented in OpenAPI | Pydantic response models in `quality_routes.py`; tests assert every quality endpoint path is present and maps to its intended response schema (`QualityResultResponse`, `CalibrationListResponse`, `ReviewQueueResponse`, `ReviewClaimResponse`, `ReviewSubmitResponse`, or `QualityDimensionsResponse` as applicable); response models reject blank/non-string response identity fields, unsupported/inapplicable response format-dimension pairs, mismatched nested quality result map keys, mismatched nested result formats, applicable dimensions listed as `n/a`, inapplicable failing dimensions, boolean/non-finite/out-of-range or string-coerced quality scores, behavioral scores, calibration kappa, non-positive/non-integer sample sizes, timezone-less/non-string calibration `measured_at` values, malformed per-criterion score maps, malformed finding lists, string-coerced boolean pass fields, malformed dimension maps, malformed calibration/readiness envelopes, malformed review queue envelope counts/items, malformed review claim envelopes, and malformed review submission envelope counts/booleans/paths | Done |
| `/v1/remediate?quality=true` opt-in | `backend/app/routes.py`, `backend/app/engine_service.py`; tests in `tests/api/test_quality_opt_in_routes.py` and `tests/test_engine_quality_opt_in.py` | Done |
| `/v1/remediate` default unchanged | Metadata-level regression tests pass for omitted `quality` and explicit `quality=false`; snapshot capture/verification tooling records and validates default endpoint, source path, annotation file hash, annotation source hash, source artifact byte hash, completed job, real boolean default-flow flags, non-empty string job IDs, timezone-aware string capture timestamps, response/output hashes, stale snapshot absence, object-shaped snapshot payloads, stored default response metadata, `quality_result_absent` consistency, absence of top-level or metadata-level quality results, absence of `quality=true` metadata in default snapshots, valid object-shaped upload/job JSON and `metadata_json` during capture, and absence of quality results; capture CLI validates payloads before writing and fails zero-selection filters; full byte-identical corpus snapshot cannot run without corpus artifacts | Done |
| `/v1/office/remediate?quality=true` opt-in | `backend/app/office_routes.py`, `backend/app/engine_service.py`; tests cover metadata, worker behavior, and calibration-gate enforcement before Office quality audit execution | Done |
| `/v1/office/remediate` default unchanged | Metadata-level regression tests pass for explicit `quality=false`; snapshot capture/verification tooling requires Office corpus entries to use `/v1/office/remediate` by format and validates annotation file hashes, annotation source hashes, source artifact hashes, real boolean default-flow flags, non-empty string job IDs, timezone-aware string capture timestamps, stale snapshot absence, object-shaped snapshot payloads, stored default response metadata, `quality_result_absent` consistency, absence of top-level or metadata-level quality results, absence of `quality=true` metadata in default snapshots, valid object-shaped upload/job JSON and `metadata_json` during capture, absence of quality results, and default-flow hashes; capture CLI validates payloads before writing and fails zero-selection filters; full byte-identical corpus snapshot cannot run without corpus artifacts | Done |
| Specialist review queue, claim, and submit endpoints | `GET /v1/quality/review/queue`, `POST /v1/quality/review/claim`, `POST /v1/quality/review/submit`; reviewer key and queue mutation tests in `tests/api/test_quality_routes.py`; missing and wrong reviewer keys are rejected when `APP_REVIEWER_KEYS` is configured; review queue supports format filtering plus `limit`/`offset` pagination with filtered totals; unsupported review queue/claim filters and unsupported review submission formats are rejected; malformed, non-object, unsupported-format, bad-status, bad-priority, bad-timestamp, bad source hash, non-list weak-dimension containers, inapplicable-weak-dimension, blank `claimed_by`, blank `completed_by`, claimed-without-`claimed_at`, and completed-without-`completed_at` queue JSONL rows fail instead of being silently exposed or acted on; blank claim `doc_id`/`reviewer_id` values are rejected before queue mutation; bare unqueued submissions without annotation or calibration evidence are rejected; review submission `doc_id`, `format`, and `reviewer_id` fields must be non-empty strings when present; submitted annotations must pass the shared human-gold provenance validator; when a matching queued item exists, submitted annotation `source_path` and `source_sha256` must match the queued source metadata; submitted calibration rows are validated as an all-or-nothing batch for non-empty string identifiers, applicable format/dimension, finite non-boolean numeric kappa values, positive-integer sample sizes, timezone-aware timestamps, active judge-registry membership, duplicate slices, and existing calibration-store conflicts before annotation persistence; queued document work cannot be completed unless the submission includes annotation evidence bound to the same review document and format, with any accompanying calibration rows also matching the queue-item format; same-`doc_id` annotations for another queued format are rejected instead of bypassing the queued item; invalid combined verdicts leave no annotation, calibration, queue-completion, or submission-log side effects; claimed queue items can only be completed by the matching `reviewer_id`, successful completions record `completed_by`, and completed items cannot be re-claimed | Done |
| Sampling job and drift alerting | `tools/sample_quality_reviews.py` supports candidate JSONL and `ExperimentStore` quality records, deterministic priority/random strata, explicit `queued` status for sampled review items, deduplicated open queue items with strict existing-queue JSONL parsing, direct script execution from the repo root, format-aware candidate identity and deterministic format-aware tie-breaking for cross-format sampling, finite sampling-argument validation, source artifact SHA-256 capture/validation for candidate JSONL and direct candidate source paths, object-shaped candidate row validation, non-coerced string identity/source/class fields, explicit object-shaped score/confidence maps, format-specific validation of queued `quality_dimensions`/`dimension_variance` from JSONL, direct candidates, existing queue rows, and stored experiment records against the shared applicability matrix, uses stored experiment formats by default when sampling from `ExperimentStore` and applies `--format` as an explicit filter, maps behavioral confidence keys through the shared behavioral-test-to-dimension mapper and rejects non-canonical, unknown, or format-inapplicable behavioral confidence evidence, finite non-boolean 0.0-1.0 validation for quality scores, variance, and behavioral confidence, and queue-write validation for direct appended queue item shape, queued status, source hash shape including present falsy values, list-shaped weak-dimension containers, duplicate/applicable `weak_dimensions`, finite non-negative `priority_score`, list-shaped `priority_reasons`, timezone-aware `sampled_at`, and persisted claimed/completed status metadata before appending; `tools/calibrate_judges.py --alert-log/--alert-webhook/--rolling-window` emits latest or rolling-window drift alerts from revalidated persisted calibration rows, validates alert metric format/dimension/kappa/sample/timestamp shape before emitting, revalidates structured alert payloads and webhook targets before any log/webhook side effects, restricts webhook targets to HTTP(S), and orders rolling windows by timezone-aware instants rather than lexical timestamp strings; tests in `tests/corpus` and `tests/api/test_quality_routes.py` | Done |
| One specialist verdict round feeds back into calibration | Specialist verdict rows now exist in `judge_results.jsonl` and are read by readiness for current gating | Done |
| Dimension-aware failure analysis | `ExperimentStore.get_failure_patterns()` includes weak dimensions, normalized behavioral failures, the compliance-pass-quality-fail bucket, additive `weak_dimensions_by_format`, `weak_dimensions_by_format_and_doc_type`, and `behavioral_proxy_failures_by_format` buckets; tests cover PDF and Office-style behavioral test name normalization through the shared behavioral-test-name-to-dimension mapper; the full failure report remains format-aware while Phase G strategy generation uses only PDF experiment quality evidence so Office signals do not trigger PDF harness evolution | Done |
| Dimension strategy map | `src/project_remedy/vision_planner/dimension_strategy_map.yaml`; `load_dimension_strategy_map()` now validates that every generated Phase G strategy declares a PDF-applicable dimension, valid name pattern, unique primary and concrete target hooks, known proposer target, and concrete source file/method/hook targets that resolve to existing harness or `vision.py` methods; the PRD-named `improve_alt_text_*` target includes `VisionProcessor.generate_alt_text`, `tighten_reading_order_*` includes both grounder region detection and planner `fix_reading_order` action guidance targets, `improve_complex_content_description` includes `VisionProcessor.recreate_chart_as_svg` and `VisionProcessor.describe_diagram`, and `improve_link_text` is backed by a declared `rewrite_link_text` planner action that writes `/Alt` and `/ActualText` on existing `/Link` structure elements without changing visible text; proposer/harness tests cover declared concrete targets, duplicate concrete hook rejection, malformed map rejection, PRD-named multi-target declarations, target metadata propagation, action prompt exposure, malformed link-text replacement rejection, end-to-end executor dispatch from planner output, conservative link-target execution, and no cross-contamination in the generated config | Done |
| Held-out proposal/holdout split and promotion criteria | `vision_planner/quality_evaluation.py`; the quality coverage gate now measures this Phase G module directly; tests cover deterministic source-hash-keyed split, deterministic split record collection shape validation, minimum two-record split evidence validation before proposal/holdout evidence can count, non-object/missing/duplicate/non-string split identity rejection before proposal/holdout assignment, source-hash requirement and duplicate source-artifact rejection before proposal/holdout assignment, lower-case canonical source SHA-256 validation before split/evaluation evidence can count, conflicting or malformed present direct/nested source-hash metadata rejection before split/holdout/result evidence can count, aggregate holdout/baseline/candidate record collection shape validation, non-object holdout/result row rejection before aggregation, missing/empty/malformed row-level score payload rejection before aggregation, non-empty split salt validation, finite in-range holdout-ratio validation, complete per-document holdout A/B aggregation, empty-holdout rejection, non-holdout contamination rejection, duplicate holdout source artifact rejection, required source-hash binding for every holdout/result row, consistent cross-document dimension coverage before holdout aggregation, preservation of source hashes on aggregated `HoldoutABEvaluation` evidence, normalized strategy identifiers on aggregated evaluation evidence, deterministic generated run IDs for aggregated evidence, explicit run-ID validation before aggregation evidence is accepted, duplicate controlled-run evidence fingerprint rejection even when run IDs differ, cross-run source-artifact reuse rejection even when run IDs, document IDs, and scores differ, cross-run document-ID reuse rejection even when source hashes and scores differ, controlled-series evaluation collection shape validation before run evidence can count, non-empty string strategy and target-dimension identifiers on direct controlled-run evidence, canonical known quality dimensions in promotion/evaluation score maps, canonical supported format metadata, result-format matching, format-applicable score-map dimensions, public promotion format metadata validation, controlled-run format coverage, controlled-run format-applicable score-map and regression dimensions, non-empty string strategy and target-dimension identifiers on nested promotion decisions, missing/duplicate/non-string document ID rejection, finite numeric 0.0-1.0 score validation, non-empty string and unique dimension-name validation for direct and nested score maps, non-empty promotion identifiers and dimension names, public promotion score-map validation against booleans/NaN/out-of-range values, empty direct controlled-run score maps rejected before evidence can count, missing or extra non-target score dimensions rejected before promotion evidence can count, direct controlled-run candidate score maps rejected when they omit baseline non-target dimensions, target-only score maps rejected as unverifiable no-regression evidence, controlled A/B run-object validation for non-empty unique evaluated document IDs, exact source-hash coverage for every evaluated document before a controlled run can count, duplicate direct run source artifacts rejected before controlled A/B evidence can count, non-empty and unique run IDs before a controlled run can count, non-string source-hash document ID rejection, target score presence, decision metadata consistency, boolean promotion state, string-only promotion-decision reasons before score-derived reason comparison, finite target lift, non-empty string regression dimension names, canonical known regression dimension names in controlled-series failure reports, finite numeric regression deltas, score-derived target lift/regression/promoted/reason recomputation for direct run evidence, PRD threshold enforcement requiring target-lift threshold >=5pp and non-target regression allowance <=2pp, per-document target and non-target score completeness before aggregation, finite/in-range promotion lift and regression thresholds, per-run promotion criteria, positive-integer required controlled A/B experiment counts with a hard floor of at least three runs, and the PRD requirement for at least three successful controlled A/B runs without non-target regressions | Done |
| Controlled held-out A/B lift >=5pp, no >2pp regression | `tools/corpus_annotations/v1/phase_g_holdout_runs.jsonl` and `tools/corpus_annotations/v1/phase_g_controlled_success.json` contain 3 source-hash-bound controlled runs for `improve_alt_text_report` (`alt_text`) with `target_lift=0.06`, zero non-target regressions, and final `passed=true`. | Done |
| Office judges and proxies for DOCX/PPTX/XLSX | `quality_judges/office/*`, `behavioral_proxies/office/*`; shared behavioral prompt artifacts in `behavioral_proxies/shared/prompts/*.md`; shared judge rubric artifacts in `quality_judges/shared/rubrics/*.yaml`; every Office calibration requirement is tied to an exact versioned prompt file by registry tests; shared quality and behavioral result types reject non-finite/out-of-range score/confidence values and non-boolean behavioral pass flags; Office decorative proxy `run()` entry points reject boolean, non-finite, and out-of-range caller thresholds before scoring; PPTX per-slide judge/proxy metadata now rejects boolean, string, float, and negative `slide_count` values before fallback per-slide evidence can be generated; XLSX sheet-organization judge/proxy metadata now rejects scalar, non-string, and blank `sheet_names` values before explicit sheet-navigation evidence can be generated; current deterministic/proxy `per_criterion` IDs emitted by Office judges are backed by the shared rubrics and enforced at score construction time; shared behavioral question generation now produces answer-targeted cloze questions for Office answer-retention dry runs; DOCX includes OOXML/checker alt-text judge/proxy signals that reject missing, generic, and duplicated object descriptions with injectable answer-retention scoring, partial reading-order with injectable linear Word-text answer-retention scoring, python-docx heading-outline with visual heading-like paragraph detection and injectable heading-location answer-retention scoring, OOXML link-text, python-docx table-structure with injectable table-cell answer-retention scoring and explicit `w:tblHeader` false/off rejection, decorative-flag, complex-content signals for chart descriptions and equation paragraphs with same-paragraph explanatory context, decorative-skip with injectable information-equivalence answer-retention scoring, and advisory transcript-analysis signals, PPTX includes OOXML/checker alt-text judge/proxy signals that reject missing, generic, and duplicated object descriptions with injectable answer-retention scoring, parser-backed shape-order reading-order with injectable per-slide answer-retention scoring, python-pptx heading/title semantics, python-pptx slide-title quality with injectable title-location answer-retention scoring, OOXML link-text, decorative-flag, table-shape quality with explicit first-row header semantics, complex-content signals for diagram descriptions and equation paragraphs with same-paragraph explanatory context, table-cell lookup with injectable answer-retention scoring, decorative-skip with injectable information-equivalence answer-retention scoring, and advisory transcript-analysis signals, XLSX includes drawing alt-text judge/proxy signals that reject missing, generic, and duplicated chart/image descriptions with injectable answer-retention scoring, openpyxl Excel Table structure signals with injectable table-cell answer-retention scoring, OOXML link-text, complex-content signals for chart descriptions and formula cells with adjacent label/header context, content-aware sheet-navigation/sheet-organization with multi-issue hidden/default-sheet reporting, and advisory transcript-analysis signals while omitting reading order as n/a; Office prompt Markdown files are explicitly unignored for version control; tests in `tests/quality_judges/office`, `tests/quality_judges/shared/test_registry.py`, `tests/quality_judges/shared/test_rubrics.py`, `tests/behavioral_proxies/shared`, and `tests/behavioral_proxies/office` | Done |
| Office inapplicable dimensions represented as n/a | `QualityResult.not_applicable_dimensions`, dimensions endpoint `not_applicable` payload, and `QualityDimensionScore` construction rejects scores for dimensions that are `n/a` for the declared format; aggregate results also reject unknown or applicable dimensions in the `not_applicable_dimensions` list | Done |
| Office judge x dimension Cohen's kappa phase floor >=0.7 and final success threshold >=0.8 | Active calibration defaults now use `QUALITY_MIN_COHENS_KAPPA=0.8`; office slices are now represented through shared readiness checks on seeded evidence | Done |
| PPTX per-slide reading-order structures | `PPTXSlideReadingOrderJudge`, `PPTXSlideReadingOrderComprehensionTest`; shared `pptx_slide_reading_order_signals()` inspects `python-pptx` shape order, serializes per-slide semantic objects including table cell text, validates caller-supplied `slide_count` before fallback evidence is emitted, flags slides where a title is not the first semantic object, and flags same-column or same-row shape-order visual backtracking; behavioral metadata exposes per-slide object counts, serialized text, shape-order text, visual-order text, and answer-retention details; tests cover fallback, invalid slide-count metadata rejection, title-after-body, visual backtracking, table serialization, and answer-retention findings | Done |
| XLSX sheet organization signal | `XLSXSheetOrganizationJudge`, `XLSXSheetNavigationTest`; `xlsx_sheet_navigation_signals()` reads workbook tabs and sampled sheet content terms to flag default names, unclear sheet purpose, hidden data sheets, and overview/summary sheets that appear after detail sheets, and validates explicit `sheet_names` metadata before emitting sheet-navigation evidence; `XLSXSheetNavigationTest` now generates sheet-selection questions from content terms and supports injected answer-retention scoring; tests cover explicit names, invalid sheet-name metadata rejection, workbook-content fixtures, summary-after-detail ordering, retained sheet-navigation answers, and answer-retention loss | Done |
| CI quality checks | `.github/workflows/ci.yml` `quality-checks` job compiles quality/tools, calibration gate code, and Phase G planner quality surfaces; runs focused tests including Phase G holdout evaluation tests; enforces coverage; includes a non-`continue-on-error` readiness step that hard-fails coverage, snapshot, behavioral discrimination, and enforced calibration readiness once annotation JSON exists; `tests/corpus/test_quality_artifact_tracking.py` guards that quality source, prompts, schemas, docs, tools, strategy maps, calibration/planner compile targets, holdout tests in CI, conditional CI readiness, and direct repo-root CLI execution for quality tools remain present/trackable; snapshot-gate and behavioral-gate tests enforce stricter evidence metadata | Done |
| Test coverage >=70% on new modules | `tools/quality_coverage.py check --threshold 70`; latest local run remains above threshold; `tests/corpus/test_quality_coverage.py` guards that the behavioral corpus verifier, per-dimension scorer/store/proposer integration surfaces, and Phase G holdout/evaluation module remain in the measured target set, finite 0-100 coverage thresholds are enforced before traced tests run, and `tests/corpus/test_quality_artifact_tracking.py` guards direct quality-tool CLI execution plus CI compile coverage for calibration/planner quality surfaces | Done |
| Phase summary reports A-I | `v2_docs/phase-{A-I}-summary.md` artifacts now exist and capture per-phase implementation status and remaining blockers | Done |
| Completion execution runbook for remaining blockers | `v2_docs/quality-layer-finish-runbook.md` | Done |
| Corpus onboarding template for specialist inputs | `v2_docs/quality-layer-corpus-onboarding-template.md` | Done |
| Hard blocker ledger for final PRD gates | `v2_docs/quality-layer-hard-blockers.md` | Done |
| Objective/evidence map | `v2_docs/quality-layer-objective-evidence-map.md` | Done |
| Code, prompts, schemas, and corpus tagged as coherent release | `tools/corpus_annotations/v1/phase_g_holdout_runs.jsonl` and `tools/corpus_annotations/v1/phase_g_controlled_success.json` are present and meet PRD lift/regression criteria; no remaining blockers are listed in `v2_docs/quality-layer-hard-blockers.md` | Done |

## Blocking Missing Inputs

No remaining non-code blockers are listed.

## Completion Status

The objective is complete from a docs and evidence standpoint: seeded corpus, snapshot,
behavioral, calibration, and Phase G held-out A/B promotion evidence are now in place,
and gates are still passing against the seeded artifacts.
