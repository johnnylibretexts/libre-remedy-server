"""Rebuild tier — regenerates a compliant PDF from extracted semantic content.

Spike scope: text-only AST + .NET QuestPDF sidecar. Not wired into the
escalation flow; only reachable via the hidden /v1/rebuild/spike endpoint.
"""
